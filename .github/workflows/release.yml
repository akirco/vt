name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install FFmpeg build dependencies
        run: |
          sudo apt-get update
          sudo apt-get purge -y libavcodec-dev libavformat-dev libavutil-dev libswscale-dev libavdevice-dev libasound2-dev || true
          sudo apt-get install -y \
            pkg-config \
            nasm \
            cmake \
            libclang-dev \
            build-essential
          sudo apt-get install -y libasound2-dev

      - name: Build (static FFmpeg)
        # run: cargo build --release
        run: cargo build --release --features ffmpeg-next/build

      - name: Verify static linking
        run: ldd target/release/vt | grep "libav" || echo "No dynamic FFmpeg libs found - OK"

      - name: Package
        run: |
          mkdir -p release
          cp target/release/vt release/vt
          tar -czvf vt-${{ github.ref_name }}-x86_64-unknown-linux-gnu.tar.gz -C release vt

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-linux
          path: vt-*.tar.gz

  create-release:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
